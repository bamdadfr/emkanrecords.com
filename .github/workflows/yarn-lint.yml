name: yarn-lint

on: push

jobs:

  run:
    runs-on: ubuntu-latest
    steps:

      - uses: actions/checkout@v2

      - uses: actions/setup-node@v1
        with:
          node-version: '12'

      - run: yarn

      - run: yarn lint

      - run: git diff

      - run: |
          git add .
          git commit -m "fix: auto linting"
      
      - run: |
          git push
      